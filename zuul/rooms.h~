#include<iostream>
#include<vector>
class room{
 public:
  room();
  char* getDescription();
  char* getItems();
  void setDescription(char* roomDescription);
  void setItems();
  vector<item*>*items = new vector<item*>();
 private:
  char* description;
  struct item{
    char* name;
    int worth;
    int weight;

  };
 



}
