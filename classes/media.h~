#ifndef MEDIA_H
#define MEDIA_H
#include<iostream>
class media{
public:
  media();
  int getType(int type);
  int getYear(int year);
  char* getTitle(char* title);
protected:
  char* title;
  int year;



};
#endif
