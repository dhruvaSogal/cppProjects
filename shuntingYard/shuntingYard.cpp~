#include <iostream>
#include "node.h"
using namespace std;
int main(){
  node* head = NULL;
  node* tail = NULL;
  //vector<char> expression;
  char[90] read;
  cout<<"Enter your math equation"<<endl;
  cin.getline(read, 90);
  
 
}

void add(node* &head, node* current, char  x   , node* tail){
  if(current->getNext() != NULL){
    add(head, current->getNext(), read, tail);

  }
  else{
    node* temp = new node()

  }
  
}
